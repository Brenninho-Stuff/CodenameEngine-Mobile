name: Android Builds

on:
  push:
  workflow_dispatch:

jobs:
  build:
    name: Android Build
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v4
      - name: Setting up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Setting up Android SDK
        uses: android-actions/setup-android@v3
      - name: Installing Android SDK components
        # NDK r25 uses clang 14 which is compatible with current hxcpp
        # NDK r21 used clang 9 which lacks __StartsWith/__EndsWith support
        run: |
          echo "y" | sdkmanager "ndk;25.2.9519653" "platforms;android-34" "build-tools;34.0.0" --sdk_root=$ANDROID_SDK_ROOT
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/25.2.9519653" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/25.2.9519653" >> $GITHUB_ENV
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.7
      - name: Restore existing build cache for faster compilation
        uses: actions/cache@v4.2.3
        with:
          # not caching the bin folder to prevent asset duplication and stuff like that
          key: cache-build-android-ndk25
          path: |
            .haxelib/
            export/release/android/haxe/
            export/release/android/obj/
      - run: |
          echo "HXCPP_COMPILE_CACHE=~/.hxcpp" >> $GITHUB_ENV
      - name: Installing/Updating libraries
        run: |
          haxe -cp commandline -D analyzer-optimize --run Main setup -s --no-vscheck
      - name: Installing Android-specific Haxe libraries
        # install AFTER setup so hxcpp isn't overwritten by an older version
        run: |
          haxelib install hxjava --quiet
          haxelib install hxcpp --quiet
          git clone https://github.com/MAJigsaw77/extension-androidtools.git /tmp/extension-androidtools
          haxelib dev extension-androidtools /tmp/extension-androidtools
      - name: Patching project.xml for Android
        run: |
          grep -q "extension-androidtools" project.xml || \
          sed -i 's|<haxelib name="markdown" />|<haxelib name="markdown" />\n\t<haxelib name="extension-androidtools" if="android" />|' project.xml
      - name: Configuring Lime for Android
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_SDK_ROOT
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_SDK_ROOT/ndk/25.2.9519653
          haxelib run lime config ANDROID_BUILD_TOOLS 34.0.0
          haxelib run lime config JAVA_HOME $JAVA_HOME
          haxelib run lime config ANDROID_SETUP true
      - name: Building the game
        run: |
          haxelib run lime build android
      - name: Uploading artifact (APK only)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine (APK Only)
          path: export/release/android/bin/app/build/outputs/apk/release/app-release.apk
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine Android
          path: export/release/android/bin/app/build/outputs/apk/release
      - name: Clearing already existing cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "cache-build-android-ndk25") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }
      - name: Uploading new cache
        uses: actions/cache@v4.2.3
        with:
          # caching again since for some reason it doesnt work with the first post cache shit
          key: cache-build-android-ndk25
          path: |
            .haxelib/
            export/release/android/haxe/
            export/release/android/obj/

# didnt compile debug in the same job or github would have said that job wasn't completed until debug was done too (debug uploads are not essential)
  debug_build:
    name: Android Debug Build
    permissions: write-all
    runs-on: ubuntu-latest
    needs: build # since its low priority, it'll run after, so actions will concentrate first on normal builds
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v4
      - name: Setting up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Setting up Android SDK
        uses: android-actions/setup-android@v3
      - name: Installing Android SDK components
        run: |
          echo "y" | sdkmanager "ndk;25.2.9519653" "platforms;android-34" "build-tools;34.0.0" --sdk_root=$ANDROID_SDK_ROOT
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/25.2.9519653" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/25.2.9519653" >> $GITHUB_ENV
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.7
      - name: Restore existing build cache for faster compilation
        uses: actions/cache@v4.2.3
        with:
          # not caching the bin folder to prevent asset duplication and stuff like that
          key: cache-build-android-debug-ndk25
          path: |
            .haxelib/
            export/debug/android/haxe/
            export/debug/android/obj/
      - run: |
          echo "HXCPP_COMPILE_CACHE=~/.hxcpp" >> $GITHUB_ENV
      - name: Installing/Updating libraries
        run: |
          haxe -cp commandline -D analyzer-optimize --run Main setup -s --no-vscheck
      - name: Installing Android-specific Haxe libraries
        run: |
          haxelib install hxjava --quiet
          haxelib install hxcpp --quiet
          git clone https://github.com/MAJigsaw77/extension-androidtools.git /tmp/extension-androidtools
          haxelib dev extension-androidtools /tmp/extension-androidtools
      - name: Patching project.xml for Android
        run: |
          grep -q "extension-androidtools" project.xml || \
          sed -i 's|<haxelib name="markdown" />|<haxelib name="markdown" />\n\t<haxelib name="extension-androidtools" if="android" />|' project.xml
      - name: Configuring Lime for Android
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_SDK_ROOT
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_SDK_ROOT/ndk/25.2.9519653
          haxelib run lime config ANDROID_BUILD_TOOLS 34.0.0
          haxelib run lime config JAVA_HOME $JAVA_HOME
          haxelib run lime config ANDROID_SETUP true
      - name: Building the game
        run: |
          haxelib run lime build android -debug
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine Android Debug
          path: export/debug/android/bin/app/build/outputs/apk/debug
      - name: Clearing already existing cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "cache-build-android-debug-ndk25") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }
      - name: Uploading new cache
        uses: actions/cache@v4.2.3
        with:
          # caching again since for some reason it doesnt work with the first post cache shit
          key: cache-build-android-debug-ndk25
          path: |
            .haxelib/
            export/debug/android/haxe/
            export/debug/android/obj/
